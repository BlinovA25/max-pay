<!DOCTYPE html>
<html>
  <head>
    <title>MaxPay</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <header>
      <nav class="navbar navbar-expand-lg bg-light">
        <div class="container-fluid" style="margin-left: 10%">
          <%= link_to "MaxPay", root_path, class: "navbar-brand" %>

          <div class="collapse navbar-collapse" id="navbarSupportedContent">
<!--            список с кнопками-->
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <li class="nav-item">
                <%= link_to "Lessons", lessons_path, class: "nav-link active" %>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="#">Link_1</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="#">Link_2</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="#">Link_3</a>
              </li>
            </ul>

            <% if @enable_search %>
              <%= form_tag "#", :method => "get", class: "d-flex", style: "margin-right: 10px" do %>
                <%= text_field_tag :search, params[:search], class: "form-control me-2" %>
                <%= submit_tag("Search", :name => nil, class: 'btn btn-outline-success') %>
              <% end %>
            <% end %>

            <div class="d-flex" role="logout" style="margin-right: 10%">
              <% if current_user %>
                <div class="nav-item dropdown">
                  <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                    <i class="bi bi-person"></i>Account
                  </a>
                  <ul class="dropdown-menu">
                    <li><a class="dropdown-item" href="#">My profile</a></li>
                    <li><a class="dropdown-item" href="#">My lessons</a></li>
                    <li><a class="dropdown-item" href="#">My payments</a></li>
                    <li><hr class="dropdown-divider"></li>
                    <li>
                      <div class="dropdown-item-text">
                        <%= link_to "Logout", destroy_user_session_path, data: { "turbo-method": :delete, turbo_confirm: 'Are you sure you want to logout?' }, class: "btn btn-danger" %>
                      </div>
                    </li>
                  </ul>
                </div>
              <% end %>
            </div>
          </div>
        </div>
      </nav>
    </header>

    <div style="margin-left: 10%">
      <%= yield %>
    </div>
  </body>
</html>
